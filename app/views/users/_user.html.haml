%tr
  %th.User-Cell= user.email
  %th.User-Cell= user.decorate.role
  - if user.admin
    %th.User-Cell
  - else
    %th.User-Cell-Button= link_to 'Destroy User', user_path(user), method: :delete, data: { confirm: "Are you sure you want to delete this?" }, class: 'btn btn-danger'





